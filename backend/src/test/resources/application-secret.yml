custom:
  jwt:
    secret-key: ${JWT_SECRET}
    expire-seconds: "#{60 * 30}"
  refreshToken:
    expire-seconds: "#{60 * 60 * 24 * 7}"
  toss:
    payment:
      secret: ${TOSS_SECRET}
spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: ${KAKAO_CLIENT_ID}
  mail:
    host: smtp.gmail.com
    port: 587
    username: gilgebolsai@gmail.com
    password: ${MAIL_PASSWORD}
    properties:
      auth-code-expiration-second: "#{3 * 60}"