custom:
  jwt:
    secret-key: ${JWT_SECRET}
    expire-seconds: "#{60 * 30}"
  toss:
    payment:
      secret: ${TOSS_SECRET}
spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: ${KAKAO_CLIENT_ID}