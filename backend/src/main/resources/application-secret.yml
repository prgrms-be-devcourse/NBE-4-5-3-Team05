custom:
  jwt:
    secret-key: ${JWT_SECRET}
    expire-seconds: "#{60 * 30}"
  refreshToken:
    expire-seconds: "#{60 * 60 * 24 * 7}"
  toss:
    payment:
      secret: ${TOSS_SECRET}
spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: ${KAKAO_CLIENT_ID}