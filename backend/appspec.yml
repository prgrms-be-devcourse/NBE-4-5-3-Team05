version: 0.0
os: linux
files:
  # Copy deploy.zip from the bundle to the target directory
  - source: /
    destination: /home/ubuntu/app

permissions:
  - object: /home/ubuntu/app/scripts/start_app.sh
    owner: ubuntu
    group: ubuntu
    mode: 755
    type:
      - file

hooks:
  ApplicationStart:
    - location: /home/ubuntu/app/scripts/start_app.sh
      timeout: 300
      runas: ubuntu